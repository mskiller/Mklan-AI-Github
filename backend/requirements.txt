# Mklan Studio — Unified Backend Requirements
# Phase 2: Backend Integration

# Core FastAPI stack
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.12,<3.0
python-multipart>=0.0.9,<1.0

# Wildcard backend dependencies
PyYAML==6.0.2
httpx>=0.28,<1.0

# Movie backend dependencies
psutil>=6.0,<7.0
requests>=2.32,<3.0
Pillow>=10.4,<12.0
huggingface_hub>=0.34,<1.0
safetensors>=0.5,<1.0
accelerate>=1.8,<2.0

# Optional: ML/AI dependencies (video generation, image gen)
# These are large — consider using requirements-ml.txt for GPU deployments
# torch>=2.7,<3.0
# diffusers>=0.35,<1.0
# transformers>=4.52,<5.0

# Testing
pytest>=8.2,<9.0
