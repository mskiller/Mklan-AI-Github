# Training Runtime Data

Training datasets, run configs, logs, and produced artifacts are created here at
runtime.

Do not commit training images, captions from private datasets, run logs, model
outputs, or archives.
